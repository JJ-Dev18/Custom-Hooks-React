# useCounter Hook


Ejemplo:

```
    const initialForm = {
        check1: false,
        check2: false,
        check3: false
    };
    
    const [ formValues, handleInputChange, reset ] = useForm( initialForm );

```